<% provide(:title, 'All Boards') %>

<% if current_user_is_admin? %>
<%= button_to "Create New Board", new_board_path, :method => :get %>
<br/><br/>
<% end %>

<table class="boards">
	<tr>
		<th>Title</th>
		<th>Topics</th>
		<th>Posts</th>
		<th>Last Post</th>
	</tr>
<%= render @boards %>
</table>

<% if current_user_is_admin? %>
<br/><br/>
<%= button_to "Create New Board", new_board_path, :method => :get %>
<% end %>