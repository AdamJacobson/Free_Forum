	<%= form_for @board do |f| %>

	<%= render 'shared/error_messages', object: @board %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<br/>

	<%= f.label :description %>
	<%= f.text_field :description %>

	<br/>

	<%= f.submit "Create", class: "button submit" %>
	<% end %>
</div>