<% provide(:title, "All Posts for #{@user.username}") %>

<% if @user.posts.any? %>
<div >
	<%= will_paginate @posts %>
	<br/><br/>

		<%= render @posts %>

	<br/>
	<%= will_paginate @posts %>
	<br/>
</div>
<% else %>
This user has no posts
<% end %>
